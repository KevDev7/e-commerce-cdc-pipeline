# Historical Synthea validation

These reports and JSON files describe the preceding Synthea implementation at commit `04bd80e`, before the Olist migration. They are preserved as historical evidence, not Olist results. Their commands and code paths refer to that older revision; use the current runbook for Olist.

The earlier paid AWS resources were deleted. Migrating the source does not turn those old tests into validation of the new Olist schema or its new DMS file layout.
